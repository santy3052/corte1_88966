def h_decimal(hexadecimal):
    decimal = int(hexadecimal, 16)
    return decimal
numero_hexadecimal = input("Ingresa un número hexadecimal: ")
decimal = hexadecimal_a_decimal(numero_hexadecimal)
print(decimal)
